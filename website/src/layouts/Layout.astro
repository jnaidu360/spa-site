<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<meta
			name="description"
			content="Relaxing massage and wellness services at Waples SPA in San Diego."
		/>
		<title>Waples SPA | Relaxing Massage & Wellness in San Diego</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
	:root {
		--color-ink: #2f2e33;
		--color-muted: #706d66;
		--color-cream: #f8f1e7;
		--color-linen: #efe2d0;
		--color-sage: #dbe0d1;
		--color-olive: #656f58;
		--color-white: #fffaf4;
		--font-display: Georgia, "Times New Roman", serif;
		--font-body: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
			sans-serif;
	}

	html,
	body {
		margin: 0;
		width: 100%;
		min-height: 100%;
		background: var(--color-white);
		color: var(--color-ink);
		font-family: var(--font-body);
		scroll-behavior: smooth;
	}

	* {
		box-sizing: border-box;
	}

	a {
		color: inherit;
	}

	img,
	video {
		display: block;
		max-width: 100%;
	}
</style>
